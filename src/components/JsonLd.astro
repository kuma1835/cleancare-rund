---
interface Props {
  daten: Record<string, unknown> | Record<string, unknown>[];
}
const { daten } = Astro.props;
---

<script type="application/ld+json" is:inline set:html={JSON.stringify(daten)} />
